node_modules
dist
dist-ssr
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# Capacitor native platforms (generate con `npx cap add`)
/ios
/android

# Mappa offline generata (vedi scripts/) — grande, non versionare
/public/tiles/*.pmtiles
/public/tiles/routing/
/tools/pmtiles
# Cache dei parziali pmtiles (intermedi, fuori dal deploy)
/.tiles-cache/
